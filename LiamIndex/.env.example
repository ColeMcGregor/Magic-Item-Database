# --- Database ---
# SQLite (default). Relative path puts the DB file in the repo root.
DATABASE_URL=sqlite:///./liamindex.db
# Example Postgres URL (uncomment and edit if you switch to Postgres)
# DATABASE_URL=postgresql+psycopg2://user:password@localhost:5432/liamindex

# --- Reddit scraping ---
# A descriptive UA is required by Reddit. Keep it non-empty.
REDDIT_USER_AGENT=liamindex-scraper/0.1 by <your-name-or-email>
# Optional OAuth credentials (leave blank to use public .json endpoints)
REDDIT_CLIENT_ID=
REDDIT_CLIENT_SECRET=

# --- Import defaults ---
# If scraper finds no image, use this local file or URL as a fallback.
DEFAULT_IMAGE_PATH=assets/default.png

# --- App behavior ---
# INFO, DEBUG, WARNING, ERROR
LOG_LEVEL=INFO
